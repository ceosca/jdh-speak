# JDH Speak Simplification — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rebrand SonicRoom to "JDH Speak" and strip it down — always-HD stereo voice, a lobby with only room + name, no "Powered by" credit, and full removal of the public-rooms / knock-to-join / vote-to-kick moderation system.

**Architecture:** Mostly deletion across an existing pnpm monorepo. Server (Express + socket.io + mediasoup, run via `tsx`) and client (React 19 + Vite + zustand) are simplified in parallel; the wire protocol only ever *loses* fields/events, so each side compiles and runs independently. zod strips unknown keys, so a lingering field from one side never errors the other during the transition.

**Tech Stack:** TypeScript, React 19, zustand, socket.io, mediasoup, Paraglide i18n, Tailwind v4.

## Global Constraints

- **Package manager:** pnpm via `corepack pnpm …` (no `pnpm` on PATH; never use npm).
- **Voice quality:** always **stereo, `maxaveragebitrate`/`opusMaxAverageBitrate` = 128000**. No opt-in.
- **Brand name:** exactly `JDH Speak` (default; `INSTANCE_NAME` env override stays).
- **Keep:** `?p2p=off` / `disableP2p` on the server (not moderation). Recording, Icecast streaming, audio-sources, ducking, chat — untouched.
- **Do NOT lower** the router's `maxaveragebitrate: 256000` ceiling in `server/src/mediasoup-config.ts` (it's a ceiling for caster/share/file).
- **i18n:** message JSON lives in `client/messages/{en,es,fr}.json`; `client/src/paraglide/**` is generated — never hand-edit. Regenerated by `pnpm dev`/`pnpm build` or `corepack pnpm --filter client exec paraglide-js compile --project ./project.inlang --outdir ./src/paraglide`.
- **Verification commands** (run from repo root):
  - Client typecheck: `corepack pnpm --filter client exec tsc --noEmit`
  - Server typecheck: `corepack pnpm --filter server exec tsc --noEmit`
  - Server tests: `corepack pnpm --filter server test`
  - Lint: `corepack pnpm lint`

---

### Task 1: Always-HD stereo voice

Remove the per-user hi-fi opt-in; voice is always stereo 128 kbps.

**Files:**
- Modify: `client/src/lib/sdp-munger.ts`
- Modify: `client/src/lib/microphone.ts`
- Modify: `client/src/hooks/useMediasoup.ts`
- Modify: `client/src/components/MicPreview.tsx`
- Modify: `client/src/components/DeviceSettings.tsx`
- Modify: `client/src/stores/room.ts`
- Modify: `client/messages/en.json`, `client/messages/es.json`, `client/messages/fr.json`

**Interfaces:**
- Produces: `forceOpusParams(sdp: string): string` (drops the `hifi` param), `microphoneConstraints(deviceId: string, voiceProcessingEnabled: boolean): MediaTrackConstraints` (drops the `hifiVoice` param). Later tasks/call-sites must use these 1-arg-shorter signatures.
- Consumes: nothing from other tasks.

- [ ] **Step 1: `forceOpusParams` always stereo 128k**

In `client/src/lib/sdp-munger.ts`, change the signature to `export function forceOpusParams(sdp: string): string` and set, unconditionally:
```ts
params.set("stereo", "1");
params.set("sprop-stereo", "1");
params.set("useinbandfec", "1");
params.set("maxaveragebitrate", "128000");
```
Update the JSDoc block to say voice is always stereo 128 kbps (drop the mono/`hifi` mentions).

- [ ] **Step 2: `microphoneConstraints` always 2 channels**

In `client/src/lib/microphone.ts`, drop the `hifiVoice` parameter and set `channelCount: 2` unconditionally. Update the comment to drop the hifi explanation.

- [ ] **Step 3: Update call-sites in `useMediasoup.ts`**

Grep `client/src/hooks/useMediasoup.ts` for `hifiVoiceEnabled` and `forceOpusParams` and `microphoneConstraints`. For each:
- `forceOpusParams(offer.sdp!, useRoomStore.getState().hifiVoiceEnabled)` → `forceOpusParams(offer.sdp!)` (and the answer call-site).
- `microphoneConstraints(deviceId, voiceProcessing, useRoomStore.getState().hifiVoiceEnabled)` → drop the 3rd arg.
- The SFU **voice** produce block (around lines 905–916) that reads `const hifiVoice = store.getState().hifiVoiceEnabled;` and conditionally sets codec options: replace with fixed `opusStereo: true` and `opusMaxAverageBitrate: 128000`, and delete the `hifiVoice` local. Leave the caster/share/file produce blocks (the ones with `opusMaxAverageBitrate: 256000`) untouched.

- [ ] **Step 4: Update `MicPreview.tsx`**

Grep `client/src/components/MicPreview.tsx` for `hifiVoiceEnabled` and drop that argument from the `microphoneConstraints(...)` call.

- [ ] **Step 5: Remove the Hi-fi toggle from `DeviceSettings.tsx`**

In `client/src/components/DeviceSettings.tsx` delete the entire "Hi-fi voice" toggle block (the `<label htmlFor={hifiVoiceId}>…</label>` + its hint `<p>`), the `hifiVoiceId = useId()` line, and the `hifiVoiceEnabled` / `setHifiVoiceEnabled` store selectors. Remove now-unused imports.

- [ ] **Step 6: Remove hifi state from the store**

In `client/src/stores/room.ts` delete: `HIFI_VOICE_KEY`, `loadHifiVoice`, the `hifiVoiceEnabled` field (interface + initial state), `setHifiVoiceEnabled` (interface + impl), and the hifi comment block. (`hifiVoiceEnabled` isn't in `reset`, so nothing to remove there.)

- [ ] **Step 7: Remove i18n keys**

In each of `client/messages/{en,es,fr}.json` delete `settings_hifi_voice_label` and `settings_hifi_voice_hint`.

- [ ] **Step 8: Verify**

Run:
```
corepack pnpm --filter client exec tsc --noEmit
corepack pnpm lint
```
Expected: PASS, no references to `hifiVoice*` remain (`grep -ri hifi client/src client/messages` returns nothing except generated paraglide, which is regenerated).

- [ ] **Step 9: Commit**

```
git add client/
git commit -m "feat: always stereo 128k voice; remove hi-fi opt-in"
```

---

### Task 2: Rebrand to "JDH Speak"

**Files:**
- Modify: `client/src/lib/branding.ts`
- Modify: `server/src/index.ts`

**Interfaces:**
- Consumes/Produces: none (constant change only).

- [ ] **Step 1: Client default name**

In `client/src/lib/branding.ts` set `export const DEFAULT_INSTANCE_NAME = "JDH Speak";`.

- [ ] **Step 2: Server default name**

In `server/src/index.ts` change the `INSTANCE_NAME` default: `const INSTANCE_NAME = process.env.INSTANCE_NAME?.trim() || "JDH Speak";`.

- [ ] **Step 3: Verify**

Run `corepack pnpm --filter client exec tsc --noEmit` and `corepack pnpm --filter server exec tsc --noEmit`. Expected: PASS. Start the app and confirm the lobby heading and browser tab read "JDH Speak".

- [ ] **Step 4: Commit**

```
git add client/src/lib/branding.ts server/src/index.ts
git commit -m "feat: rebrand default instance name to JDH Speak"
```

---

### Task 3: Remove the "Powered by" credit

**Files:**
- Delete: `client/src/components/Footer.tsx`
- Modify: `client/src/components/Lobby.tsx`
- Modify: `client/src/components/Room.tsx`
- Modify: `client/messages/en.json`, `client/messages/es.json`, `client/messages/fr.json`

**Interfaces:**
- Produces: `Footer` and `PoweredBy` no longer exist — Task 4 (Lobby) and any Room edits must not import them.

- [ ] **Step 1: Delete the Footer component**

Delete `client/src/components/Footer.tsx`.

- [ ] **Step 2: Remove usages in `Room.tsx`**

In `client/src/components/Room.tsx` remove `import { Footer, PoweredBy } from "./Footer";`, every `<Footer />` (connecting/error screens), and the `<PoweredBy />` inside the room controls `<footer>`. Keep the `<footer>` landmark element itself (now without the credit).

- [ ] **Step 3: Remove usage in `Lobby.tsx`**

In `client/src/components/Lobby.tsx` remove `import { Footer } from "./Footer";` and the `<Footer />` at the end. (Lobby is gutted further in Task 4 — this just clears the footer.)

- [ ] **Step 4: Remove i18n key**

Delete `footer_powered_by` from `client/messages/{en,es,fr}.json`.

- [ ] **Step 5: Verify**

Run `corepack pnpm --filter client exec tsc --noEmit` and `corepack pnpm lint`. Expected: PASS, no remaining import of `./Footer`.

- [ ] **Step 6: Commit**

```
git add client/
git commit -m "feat: remove Powered by SonicRoom credit"
```

---

### Task 4: Strip the lobby to room + name only

**Files:**
- Modify: `client/src/components/Lobby.tsx`

**Interfaces:**
- Consumes: `Footer` already removed (Task 3).
- Produces: room URLs no longer carry `?public=true` / `?p2p=off` / `?mic=off` from the lobby.

- [ ] **Step 1: Gut the lobby form**

Rewrite `client/src/components/Lobby.tsx` to keep only: the language-less header (`getInstanceName()` heading + `Headphones` icon), `m.lobby_tagline()`, the **room** input, the **display-name** input, the inline error `<p>`, and the submit button. Remove:
- State + helpers: `disableP2p`/`isP2pDisabled`, `makePublic`/`isPublicEnabled`, `joinWithoutMic`/`isMicDisabled`, all `publicRooms`/`activeRoomIdx`/`announcement`/`announceSeq` state, `PUBLIC_ROOMS_POLL_MS`, the public-rooms `useEffect` fetch/poll, `selectPublicRoom`, `onRoomListKeyDown`, `roomOptionRefs`, `listFmt`, `activeRoomId`, and the SR live-region `<div>`.
- JSX: the three checkbox blocks, the public-rooms `<ul role="listbox">` block, `<MicPreview />`, `<LanguageSelect />`.
- `handleJoin`: keep room/name validation + `sessionStorage.setItem("sonicroom:displayName", trimmedName)`; navigate to `/room/${sanitizedRoom}` with **no** query params.
- Imports: drop `Globe`, `DoorOpen`, `MicPreview`, `LanguageSelect`, `getLocale`. Keep `Headphones`, `ArrowRight`, `getInstanceName`, `m`, router hooks, and `sanitize`. Keep the `?room=` prefill + focus behaviour.

- [ ] **Step 2: Verify**

Run `corepack pnpm --filter client exec tsc --noEmit` and `corepack pnpm lint`. Expected: PASS. Start the app: the lobby shows only the two fields + button, no checkboxes, no language picker, no mic preview, no public list.

- [ ] **Step 3: Commit**

```
git add client/src/components/Lobby.tsx
git commit -m "feat: lobby asks only for room and display name"
```

---

### Task 5: Remove moderation from the server

Remove public rooms, knock-to-join, and vote-to-kick from the backend.

**Files:**
- Modify: `server/src/room-manager.ts`
- Modify: `server/src/signaling.ts`
- Modify: `server/src/index.ts`
- Delete: `server/src/kick-util.ts`, `server/src/kick-util.test.ts`
- Delete: `server/src/notify.ts`
- Modify: `.env.example`

**Interfaces:**
- Produces: the join ack no longer includes `isPublic` or `kickVotes`; `Peer` no longer has `ip`; `Room` no longer has `isPublic`/`pendingJoins`/`admittedTokens`/`admittedNames`/`bannedIps`/`kickVotes`; events `join-requests`, `join-approved`, `join-denied`, `room-public`, `kick-vote`, `peer-kicked`, `you-were-kicked` are never emitted; socket events `join-decision`, `vote-kick` are no longer handled. Task 6 (client) relies on all of these being gone.

- [ ] **Step 1: Trim `room-manager.ts`**

In `server/src/room-manager.ts`:
- Remove from the `Room` interface and from `getOrCreateRoom`'s literal: `isPublic`, `pendingJoins`, `admittedTokens`, `admittedNames`, `bannedIps`, `kickVotes` (and their doc comments).
- Remove `ip` from the `Peer` interface, and the `ip` parameter from `createPeer` (and the `ip` assignment).
- Delete the `getPublicRooms` function.
- Keep `casters`, `sharers`, `fileStreamers`, ducking fields, `messages`, `disableP2p`.

- [ ] **Step 2: Trim `signaling.ts`**

In `server/src/signaling.ts`:
- Remove imports `kickThreshold` (from `./kick-util.js`) and `notifyPublicRoomCreated, notifyPublicRoomJoin` (from `./notify.js`).
- `joinSchema`: remove `isPublic` and `joinToken`.
- Remove `clientIp` (now unused) and the `kickLimiter`.
- Remove helpers: `broadcastJoinRequests`, `votablePeerCount`, `cleanupKickVotes`, `kickPeer`, `settleKicks`.
- `join` handler: remove `ip` capture, the `bannedIps` check, `wasPublic`, the `alreadyAdmitted`/knock gate (the whole `if (wasPublic && …) { pendingJoins.set … cb({status:"pending"}) return }` block), the `admittedTokens`/`admittedNames` writes, `pendingRequest`, the `isPublic` sticky-set, the `notify*` calls + `room-public` emit, the "newcomer sees pending knocks" `join-requests` emit, and from the `cb({...})` ack remove `isPublic` and `kickVotes`. `createPeer(room, socket.id, displayName)` now takes no ip.
- Remove the `join-decision` and `vote-kick` socket handlers entirely.
- `teardownPeer`: remove the `cleanupKickVotes(room, peerId)` call and the `else if (room.pendingJoins.size > 0) { … }` branch.
- `disconnect`: remove the `pendingRequest` block, `kickLimiter.forget(socket.id)`, and the `settleKicks(room)` call.
- Remove the `let pendingRequest: Room | null = null;` declaration.

- [ ] **Step 3: Remove the public-rooms route**

In `server/src/index.ts` remove `getPublicRooms` from the `./room-manager.js` import and delete the `app.get("/api/public-rooms", …)` route.

- [ ] **Step 4: Delete dead files**

```
git rm server/src/kick-util.ts server/src/kick-util.test.ts server/src/notify.ts
```

- [ ] **Step 5: Clean `.env.example`**

Remove the `NOTY_*` lines from `.env.example`.

- [ ] **Step 6: Verify**

Run:
```
corepack pnpm --filter server exec tsc --noEmit
corepack pnpm --filter server test
corepack pnpm lint
```
Expected: typecheck PASS; tests PASS (kick-util test is gone; recording/streaming/etc. stay green); lint PASS with no unused symbols.

- [ ] **Step 7: Commit**

```
git add server/ .env.example
git commit -m "feat: remove public rooms, knock-to-join, and vote-to-kick (server)"
```

---

### Task 6: Remove moderation from the client

**Files:**
- Modify: `client/src/stores/room.ts`
- Modify: `client/src/hooks/useMediasoup.ts`
- Modify: `client/src/components/Room.tsx`
- Modify: `client/src/components/ParticipantCard.tsx`
- Modify: `client/src/lib/sounds.ts`
- Delete: `client/src/components/JoinRequests.tsx`
- Modify: `client/messages/en.json`, `client/messages/es.json`, `client/messages/fr.json`

**Interfaces:**
- Consumes: the server no longer emits any moderation events (Task 5).

- [ ] **Step 1: Trim the store**

In `client/src/stores/room.ts` remove: the `JoinRequest` interface; the fields `joinRequests`, `awaitingApproval`, `roomIsPublic`, `kicked`; `PeerState.kickVotes` and `PeerState.iVotedKick`; the actions `setJoinRequests`, `setAwaitingApproval`, `setRoomIsPublic`, `setKicked`, `setPeerKickVote` (interface + impl); the `kickVotes: 0, iVotedKick: false` init in `addPeer`; and the four moderation entries in the initial state and in `reset` (`joinRequests`, `awaitingApproval`, `roomIsPublic`, `kicked`).

- [ ] **Step 2: Trim `useMediasoup.ts`**

In `client/src/hooks/useMediasoup.ts`:
- Remove the socket handlers for `join-requests`, `join-approved`, `join-denied`, `room-public`, `kick-vote`, `peer-kicked`, `you-were-kicked` (grep each event name).
- In the `join` emit payload, stop sending `isPublic` and `joinToken`; remove any `joinToken` sessionStorage read/write.
- Remove the `status === "pending"` / `awaitingApproval` handling in the join-response path and any `vote-kick` emit helper.
- Remove now-unused store setters and imports.

- [ ] **Step 3: Trim `Room.tsx`**

In `client/src/components/Room.tsx` remove: the `JoinRequests` import + render; the `awaitingApproval` "waiting to be let in" screen; the `kicked` "you were removed" screen; and the knock-cue playback (grep the sound name from Step 5). Remove now-unused store selectors/imports.

- [ ] **Step 4: Trim `ParticipantCard.tsx`**

In `client/src/components/ParticipantCard.tsx` remove the vote-to-kick button and its `aria-pressed`/`aria-selected`/tally accessible-name logic, plus any `kickVotes`/`iVotedKick`/vote-callback props and the `roomIsPublic`/votable gating. Keep the rest of the card.

- [ ] **Step 5: Remove the knock cue**

In `client/src/lib/sounds.ts` remove the knock cue export and its data/usage (grep `knock`).

- [ ] **Step 6: Delete `JoinRequests.tsx`**

```
git rm client/src/components/JoinRequests.tsx
```

- [ ] **Step 7: Remove i18n keys**

Grep `client/messages/en.json` for keys referencing knock / join request / kick / vote / public room (e.g. `lobby_public_room*`, `lobby_make_public*`, `lobby_disable_p2p*`, `lobby_join_without_mic*`, and any `join_request*` / `kick*` / `you_were*` / `awaiting*` keys). Remove the same keys from all three `client/messages/{en,es,fr}.json` at parity.

- [ ] **Step 8: Verify**

Run:
```
corepack pnpm --filter client exec tsc --noEmit
corepack pnpm lint
```
Expected: PASS, no references to removed events/keys (`grep -rinE "kick|knock|join-request|room-public|awaitingApproval|roomIsPublic" client/src` returns nothing meaningful outside generated paraglide).

- [ ] **Step 9: Commit**

```
git add client/
git commit -m "feat: remove public rooms, knock-to-join, and vote-to-kick (client)"
```

---

### Task 7: Full end-to-end verification

**Files:** none (verification only).

- [ ] **Step 1: Static gates**

Run all four and confirm PASS:
```
corepack pnpm --filter client exec tsc --noEmit
corepack pnpm --filter server exec tsc --noEmit
corepack pnpm --filter server test
corepack pnpm lint
```

- [ ] **Step 2: Manual smoke test**

Start the app (`start.bat` or the two `corepack pnpm … exec` commands). In `http://localhost:5173`:
- Lobby shows only room + name + button; heading and browser tab read "JDH Speak"; no footer credit.
- Open two tabs in the same room → they hear each other (P2P ≤2, SFU at 3+).
- In `chrome://webrtc-internals` (or the SDP) confirm voice is **stereo** with `maxaveragebitrate=128000`.
- No UI anywhere for making a room public, admitting knocks, or voting to kick.

- [ ] **Step 3: Final note**

Leave the branch `jdh-speak` ready for review/merge (see superpowers:finishing-a-development-branch). No commit needed for this task.
```

```

## Self-Review

- **Spec coverage:** Change 1 → Task 1; rename → Task 2; Change 3 (Powered by) → Task 3; Change 2 (lobby) → Task 4; Change 4 server → Task 5; Change 4 client → Task 6; verification → Task 7. All spec sections covered.
- **Placeholders:** none — each step names exact files/identifiers/commands. Deletion-heavy steps reference symbols by name + grep rather than reproducing large blocks (the executor opens the file), which is accurate for removals.
- **Type consistency:** `forceOpusParams(sdp)` and `microphoneConstraints(deviceId, voiceProcessingEnabled)` new signatures defined in Task 1 and used consistently; `createPeer(room, id, displayName)` (no ip) consistent between Task 5 Step 1 and Step 2.
