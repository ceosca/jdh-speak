# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
*.tsbuildinfo

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Environment
.env
.env.local
.env.*.local

# Editor / OS
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Vite
.vite/

# Coverage / cache
coverage/
.cache/
.eslintcache
.parcel-cache/

# Runtime
*.pid
*.seed
*.pid.lock

# Local Claude Code dev config (preview launch.json, etc.)
.claude/

# Operator-provided event sounds: the audio files are per-deployment (dropped on
# the server), so they're not versioned — but keep the README that documents them.
sounds/*
!sounds/README.md

# Operator-provided live-TV channel list (may hold DRM keys) + the standalone tool
/tv/*
!/tv/README.md
# db.json holds DRM ClearKeys — versioned ONLY because this repo is PRIVATE.
# If the repo is ever made public again, remove this line and purge it from history.
!/tv/db.json

# Serieteca reference material (series.json is fetched from archive.org at runtime)
/serieteca/

# Extra ambience impulse responses live next to the built-ins in
# client/public/ir/ (AMBIENCE_IR_DIR). The built-in .ogg impulses are tracked;
# ignore heavy raw packs an operator drops there so they can't be committed by
# accident (they'd bloat the repo and client/dist). See server loadAmbiences.
client/public/ir/*.wav
client/public/ir/*.flac
client/public/ir/*.aac
client/public/ir/*.m4a
